# LEDGER, Inkling-turbo

Every number here is measured-or-null. `null` = not yet measured. No estimates in this file, ever.
Every non-null number names its evidence, and its evidence class. The strong class is an artifact under `journal/remote/` that a reader can re-parse: JSON, a raw log, a profiler export. The weak class is a number transcribed into a journal session with no machine-readable record behind it. Both are allowed; mixing them without saying which is which is not, because it lets a reader assume every number here is checkable. Weak-class numbers are marked **journal-only** in the cell that carries them, so the marker is the authority and no count is repeated here to drift out of date. `grep -n "journal-only" LEDGER.md` enumerates them. Everything else cites an artifact. See [journal/remote/README.md](journal/remote/README.md) for what each artifact is and [docs/METHODOLOGY.md](docs/METHODOLOGY.md#measured-or-null-ledger) for the rule.
Baseline reference (external claim, NOT ours): vLLM day-0 blog reports 380 tok/s/user (MTP8) / 140 tok/s/user (no MTP) on 4x GB200 TP8, 8K in / 1K out.

## E2E serving (remote, vs stock day-0 build, same checkpoint/quant/GPUs/SLO)

Measured 2026-07-25 on **8x H200**, TP8, the real 592GB NVFP4 checkpoint, both
builds `--max-model-len 3072 --gpu-memory-utilization 0.94 --enforce-eager
--seed 0`. Rows are medians. The offered-concurrency label is `--max-concurrency`
and NOT a server batch size; see the note under the table.

| Metric | Stock day-0 | Inkling-turbo | Delta | Evidence |
|---|---|---|---|---|
| Output tok/s, prefill-heavy @ conc 8 | 63.709 | 70.453 | **1.106x** | 3 runs vs 3, [e2e_s30_h200](journal/remote/e2e_s30_h200/) |
| Request/s, prefill-heavy @ conc 8 | 0.498 | 0.550 | **1.106x** | 3 runs vs 3, same |
| TTFT p50, prefill-heavy @ conc 8 | 377.961 ms | 340.199 ms | 1.111x, **not claimed** | 3 runs vs 3, same. The medians favour ours but the ranges overlap heavily, stock [369.979, 884.773] against ours [338.328, 862.342]: one cold-start run per build swamps the gap. See the note below the table |
| TPOT p50, prefill-heavy @ conc 8 | 117.863 ms | 106.566 ms | **1.106x** | 3 runs vs 3, same |
| p99 end-to-end, prefill-heavy @ conc 8 | 17492.6 ms | 15975.5 ms | 1.095x, **not claimed** | 3 runs vs 3, same. Ranges overlap here, unlike the other two p99 rows: stock [15460.1, 18376.1] against ours [13906.2, 16625.8]. A p99 over a 3-minute run at concurrency 8 is one request's tail |
| Output tok/s, decode-heavy @ conc 8 | 68.008 | 75.266 | **1.107x** | 3 runs vs 3, same. Was 3-vs-1 and labelled indicative; the median did not move when the two missing runs landed |
| Request/s, decode-heavy @ conc 8 | 0.066 | 0.074 | **1.107x** | 3 runs vs 3, same |
| TPOT p50, decode-heavy @ conc 8 | 117.350 ms | 106.048 ms | **1.107x** | 3 runs vs 3, same |
| p99 end-to-end, decode-heavy @ conc 8 | 120655.0 ms | 109170.6 ms | **1.105x** | 3 runs vs 3, same |
| Output tok/s, prefill-heavy @ conc 1 | 8.641 | 9.588 | **1.110x** | 3 runs vs 3, same |
| Request/s, prefill-heavy @ conc 1 | 0.068 | 0.075 | **1.110x** | 3 runs vs 3, same |
| TPOT p50, prefill-heavy @ conc 1 | 115.429 ms | 103.671 ms | **1.113x** | 3 runs vs 3, same |
| p99 end-to-end, prefill-heavy @ conc 1 | 14894.3 ms | 13630.8 ms | **1.093x** | 3 runs vs 3, same |
| Anything decode-heavy @ conc 1 | 1 run | **none** | null | `ours` never ran decode at conc 1: one run costs ~32 min, 16 prompts x 1024 tokens strictly sequential, and the deadline refused it. The one stock run is not a comparison |
| Throughput @ bs8 / bs32 / bs128 | null | null | null | **These rows cannot be filled from this data and the bs128 row cannot be filled on this hardware at all.** `--max-concurrency` is what the client offers, not the server's batch. The server reports its own ceiling as 61.25x here, so 8 is genuinely batched, but it is still a different quantity |

**Read the size of the delta.** The attention kernel is 2.66x faster in isolation
at batch-1 64K decode. End to end it buys about **10%**, because attention is a
slice of serving time and the MoE layers and the big GEMMs dominate. This
repository said that before it had a number; now it has one, and the number is
1.10x, not 2.66x. Quoting the kernel ratio as a serving ratio overstates by 2.4x.

**Cross-container, and here is why that is still defensible.** The two builds ran
in separate containers because the first spent its whole allowance on stock. Both
servers profiled memory independently and arrived at the identical KV budget,
188160 tokens and a 61.25x reported concurrency ceiling, drift 0.0000%. The KV
pool is the thing that would otherwise confound this, and the harness refuses to
publish a comparison whose pools drift more than 2%.

**Every ours run beat every stock run, which is stronger than comparing medians.**
On output tok/s and on TPOT p50, in all three matched comparisons, the slowest of
the three `ours` runs is still faster than the fastest of the three `stock` runs.
The intervals do not touch. The three ratios independently land at 1.106x, 1.107x
and 1.110x across two mixes and two concurrencies, which is why this is quoted as
"about 10%" rather than to three decimal places. Where the intervals **do** overlap
the number is not claimed: TTFT is the case, at both concurrencies and in both
mixes, because a cold-start outlier in one run of each build swamps the difference.
TTFT p99 on `ours` at conc 1 spans 130 ms to 1103 ms across three runs.

**Generated-token equivalence, for the first time in this project.** Four fixed
prompts per build, temperature 0, `max_tokens=32`, `--ignore-eos` deliberately
off: **4 of 4 completions byte-identical between stock and ours.** Every earlier
"tokens matched" claim in this repository was `max_tokens=0` with `echo=True`,
which compares echoed prompt positions and is near-tautological under one
tokenizer. These are real generated tokens.

`--enforce-eager` on both sides, so no CUDA graphs. H200 rather than H100: same
architecture, sm_90, so the kernel is the one the microbenchmarks measured, and
what differs is headroom. 8x143GB holds 61 concurrent maximum-length requests
against 8x80GB holding 1.43, which is why the H100 attempt could not produce a
batched comparison at all.

The earlier 8x H100 attempt lost its results to a watchdog race in our own
orchestration and is written up in
[journal/u2-hopper-design.md](journal/u2-hopper-design.md#session-28-postscript-e2e-curves-lost-to-a-watchdog-race-orchestrator-error).
Nothing partial was ever reported from it.

## Kernel gates (ncu, >=90% of binding roofline)

| Unit | Kernel | Binding roofline | % achieved | Parity 32/32 | batched==bs1 | Profile |
|---|---|---|---|---|---|---|
| U1 | fused NVFP4 MoE grouped GEMM | null | null | null | null | null |
| U2 | rel-attn prefill | ncu ran, 90% gate NOT met (SM SOL 45.6%, MemSOL 55.9%, occupancy 14.0% at prefill 8k; decode b1 is parallelism-bound at 64 CTAs on 132 SMs, DRAM 7.2%). **journal-only**: these four percentages were read off Nsight Compute and transcribed into journal session 24. The `.ncu-rep` files are gitignored, so no artifact under `journal/remote/` carries them. **The prefill figures stand. The decode DRAM 7.2% and the decode occupancy were profiled on the kernel carrying the bias-shift defect, so they were measured with most of the decode bias traffic absent and need re-profiling; the 64-CTAs-on-132-SMs figure is a launch geometry and is unaffected** | below gate, ceiling documented in journal session 24 ncu | per-op 3/3 on sm_90+sm_80+sm_120, **all cases `cu_seqlens_q == cu_seqlens_k`, so this certifies the full-prefill family and nothing else**; FULL-MODEL 32/32 greedy-token match vs stock on 8x H100 (2026-07-24), **on echoed PROMPT positions: the stock-against-ours comparison ran `max_tokens=0` with `echo=True`, so no generated token was ever compared across builds. Scope it to that comparison: the same-build batch-consistency controls in the same script DID generate 32 tokens per prompt, 128 of their 348 positions per build, so the cross-build number and the noise floor it is read against do not share a position mix**; logprob-tolerance gate fail-as-specified with control (noise floor) also failing - see journal session 28 | batched==bs1 FAILS ON STOCK ITSELF (platform nondeterminism, documented) | session 24: ~~decode b1 kv64k 905.6us vs day-0 score_mod 2375 (2.6x)~~ **WITHDRAWN 2026-07-25, the bias-shift defect made this not like-for-like, see journal/regression-sm90-bias-shift.md. The 905.6 stands as a timing and traces to the session-24 artifact. The 2375 does NOT come from session 24: that artifact's own `decode_b1_global_kv64k` score_mod reading is 2432.98, and 2375 is the session-1 figure (2374.9, journal/remote/h100-session1.md). So this cell also silently crossed two sessions, found 2026-07-25 while verifying the withdrawal. The ratio is withdrawn on both counts**; prefill 8k 3362us vs day-0 score_mod 5368 (1.6x), unaffected. Also measured on the same box: relprojT 4163 / relproj 6209 at decode and 8483 / 13049 at prefill, but those are OUR abandoned V1/V1.5 prototypes, NOT day-0 paths, and no speedup is claimed against them. journal/remote/microbench_attn_day0_native_sm90_session24.json + microbench_attn_scoremod_session24.json |
| U2 | rel-attn, session-25 reproduction (different box, torch 2.11/cu130) | see row above | see row above | per-op 3/3 native sm_90, max 1.56e-2 (**journal-only**: the 1.56e-2 is transcribed from journal session 25, `parity_fa4_rel.py` prints it and writes no artifact) | not re-run | vs the only day-0 path (score_mod). **Standing:** prefill_global_8k 3308.8 vs 4799.4 (1.45x faster); **prefill_swa_8k 1223.0 vs 956.5 = 1.28x SLOWER, we lose this case**. Both are `seqlen_q == seqlen_k`. **WITHDRAWN 2026-07-25, ~~decode b1 kv64k 852.6 vs 2326.6 (2.7x)~~ and ~~decode b32 854.8 vs 2391.2 (2.8x)~~ and ~~decode b32 kv8k 124.1 vs 304.4 (2.5x)~~**: our sm_90 kernel computed its sheared-bias shift as `padded//tile_n - (128*(m_block+1))//tile_n`, the `seqlen_q == seqlen_k` specialisation, so at these shapes it applied the bias to 1 KV block instead of 10 (1 of 512 at b1 kv64k, and the oldest block) while score_mod gathered every score. Not like-for-like, so the ratios are not results. The six timings themselves stand as timings. Re-measured post-fix in the row below, at 2.66x / 2.75x / 2.10x, so the claim survives lower rather than collapsing; these rows stay withdrawn anyway, because a number that lands near a sound one by accident was still unfounded. Write-up: journal/regression-sm90-bias-shift.md. Reference points, not baselines: plain no-bias 736.0 (decode b1); our abandoned relprojT 5154.7 / relproj 7194.5. True 32-seq decode kv64k 13821.4 (431.9/seq), also a defective-kernel decode timing and withdrawn as a statement about decode cost. journal/remote/microbench_attn_day0_session25_h100.json + microbench_attn_scoremod_session25_h100.json |
| U2 | rel-attn post-fix re-measurement, sm_90 (H100, 2026-07-25, one container per build, `u2_serving_route` only, u3 and shear fusion asserted absent) | see U2 prefill row | see U2 prefill row | **`parity_rel_chunked_decode` 7/7**, the first `seqlen_q != seqlen_k` correctness result on any arch: chunked prefill 128-on-1408 and 256-on-768, decode ctx2047 and ctx4095, decode SWA ctx4095, chunked SWA 128-on-1408, plus a `seqlen_q == seqlen_k` control. Worst mean abs diff 6.96e-05, worst max 7.81e-03 which is the bf16 quantum on the control. `parity_fa4_rel` still 3/3, `parity_qkvr_prep` green. **Also run against a deliberately re-broken kernel** (`128*(m_block+1)` put back): 6 of 6 defective cases separate on the mean, worst legitimate 6.96e-05 against best defective 3.28e-03, a factor of 47, and the control row is bit-identical in both runs. That control exposed a tolerance hole: at the original `TOL_MEAN` 5e-3 the broken `decode_global_ctx4095` case passed at 3.28e-03, so `TOL_MEAN` is now 5e-4. Artifacts: journal/remote/validate_s27_decodefix/ and journal/remote/validate_s27_brokencontrol/ | not re-run | vs day-0 `score_mod`, both timed in the same container, us/iter: decode b1 kv64k **894.7 vs 2375.6 = 2.66x faster**; decode b32 kv64k **867.6 vs 2389.4 = 2.75x**; decode b32 kv8k **146.3 vs 307.7 = 2.10x**; prefill_global_8k **3353.7 vs 4820.8 = 1.44x**; **prefill_swa_8k 1224.5 vs 965.4 = 1.27x SLOWER**. Reference point, not a baseline: plain no-bias 736.0 at b1, so correct bias costs 21.6% there. True 32-seq decode kv64k 14697.5 (459.3/seq). Cost of the fix, broken control against fix, two containers of the same box class: decode b1 852.3 to 894.7 (+5.0%), decode b32 kv64k 851.8 to 867.6 (+1.9%), decode b32 kv8k 122.2 to 146.3 (+19.7%), prefill global 3329.7 to 3353.7 (+0.7%), prefill swa 1210.6 to 1224.5 (+1.1%); the two prefill figures are the extra `get_n_block_min_max` call alone, since the shift is arithmetically unchanged there. **One container only, so decode is not yet reproduced on a second machine.** journal/remote/validate_s27_decodefix/microbench_ours_attn_shearfusion_OFF_modal_h100x1_route.json + microbench_attn_scoremod_modal_h100x1_route.json |
| U2 | rel-attn on sm_80 (A100) | null | null | per-op 3/3 (2026-07-23), **all three cases `cu_seqlens_q == cu_seqlens_k` and all three single-sequence, so this certifies single-sequence full prefill and nothing else**, and it was measured on the pre-fix file | null | no speedup ratio exists: every day-0 path raises NotImplementedError on SM8x, so there is no baseline. **Support claim only, and as of 2026-07-25 it is measured on Ampere rather than inferred.** On a verified A100, capability (8,0) asserted: `parity_rel_chunked_decode` **7/7** with per-case power 6.7x to 37.7x `TOL_MEAN`, `parity_rel_bias_coverage` **6/6**, `parity_rel_varlen_batch` **12/12**, `parity_fa4_rel` 3/3 on our arm, `parity_qkvr_prep` 5/5. Two defects were found and closed on the way: an unpredicated bias copy that faulted with `cudaErrorIllegalAddress` on multi-sequence batches, and a `pack_gqa` shear-granularity mismatch that reached the production `Hq=64` over `Hkv=8` geometry and was observed on A100 at mean 1.9666e-03 before the guard. journal/remote/validate_a100x1_s31/, journal/remote/validate_a100x1_s32_packgqa/, journal/regression-pack-gqa-shear-granularity.md. **Still not a serving claim on that architecture, for a memory reason rather than a correctness one**: 8x A100 40GB is 320GB against a 592GB checkpoint, so Inkling has never been served end to end on Ampere at all **TILE SWEEP WITHDRAWN 2026-07-25**: ~~tile_n=32 b1 decode 5350.1us vs untuned tile_n=64 5953.7 = 10.1% faster~~; ~~32-seq 60801.4 vs 74356.6 = 18.2% faster~~; ~~post-deploy re-validation on A100, 32-seq 60977.5 vs 75013.4 = 18.7% faster~~ (that last one **journal-only**, session 27, no separate JSON). Reason: `harness/tune_sm80.py` times decode shapes (T_q=1 against T_k=65536, `:48-49`, constructed at `:65-66` and `:73-74`) while its `parity_ok()` built ONE cu_seqlens and passed it as both cu_seqlens_q and cu_seqlens_k (`:102`, `:105-106`), so the gate verified `seqlen_q == seqlen_k` and the generic kernel was wrong on exactly the family its own gate never exercised. The tile size was selected under a reader addressing the bias outside its own tile domain at the timed shapes. WHAT SURVIVES: the four absolute timings per config stay in journal/remote/tune_sm80_a100.json, both **prefill** rows are 8192-on-8192 and are unaffected (so tile_n=64 winning SWA prefill 9175.2 vs 10565.6 still stands, and it is the "otherwise 64" half of the shipped rule), and the tile_n=128 collapse survives. The specialisation was fixed in `flash_fwd.py` in `9b63979`; re-reading that found it dimensionally wrong for tile_n != 128, which is every tile the generic path selects, and `b5f0f7e` superseded it. **The corrected form has since executed on an sm_120 5090 (2026-07-25) and has still never executed on an A100**, so sm_80 has NO correctness result for the code in the tree, on any shape family. See the sm_120 row below for what did run. journal/regression-ampere-tile-sweep.md |
| U2 | rel-attn on sm_120 (RTX 5090 Laptop, local, 2026-07-25), first execution of the generic kernel's corrected shear shift on any silicon | null | null | **journal-only, and the reason is that the three JSONs this session committed under `journal/remote/local_sm120_s29/` are zero-byte files** (`git ls-tree -l HEAD` reports the empty blob `e69de29` for each), and `parity_fa4_rel` and `parity_qkvr_prep` wrote no file at all. So every count in this cell is transcribed from journal/regression-sm120-varlen-illegal-address.md with nothing re-parseable behind it. `parity_rel_chunked_decode` **7/7**, and every case carries a per-case signal 7.5x to 37.8x above tolerance, so each one could have failed, which is the rule-8 test. `parity_fa4_rel` **3/3** for `tml_fa4_rel_bias`. `parity_rel_bias_coverage` **6/6**. `parity_qkvr_prep` **5/5**. **`parity_rel_varlen_batch` 1/12**, `cudaErrorIllegalAddress` on the first multi-sequence case, which is the open defect in the row under Reproducibility incidents. `parity_rel_paged` not run. This is the first `seqlen_q != seqlen_k` correctness result the generic path has ever had; it does not transfer to sm_80, which is different silicon | null | no speedup ratio is claimed. RTX 5090 Laptop is power-capped and on WDDM, and this session took no timings. The pre-existing "2% to 10% faster than day-0" sm_120 figures are older, **journal-only**, and unchanged by this session. journal/regression-sm120-varlen-illegal-address.md |
| U2 | rel-attn shear fusion (`u2_shear_fusion.py`: qkvr_prep writes the sheared layout, ShearingBias and its two scheduler kernels removed) | null | null | sm_90 (H100, session 26): **14/16**. All 14 writer cases bit-exact vs stock ShearingBias. Both attention cases FAILED: `NameError: cannot access local variable 'n_block'` in `flash_fwd_sm90.py`, so the pre-sheared `bias=` path cannot execute on Hopper at all. sm_120 (RTX 5090): 16/16, one run, **journal-only**, the only record is commit `7375849`; that gate wrote no file. NOT run on sm_100. Artifact: journal/remote/validate_s26_h100x1/ | null | **MEASURED, and it is a net LOSS on prefill.** Fused writer vs natural writer + ShearingBias, same inputs, one process, per-kernel: global prefill 8K 2336.1 vs 1312.1 = **+1019.4us**; SWA prefill 8K 1251.6 vs 685.9 = **+561.1us**; decode b32 kv64k 10.3 vs 10.8 = -4.7us, a small win. **Caveat found 2026-07-25: that decode row's three cells use two bases and 10.8 minus 10.3 is not 4.7.** The 10.8 and 10.3 are two-kernel sums; the -4.7 is a totals difference that also drops a `Memcpy DtoH` and a `reduce_kernel` worth 4.25us which only the natural path launches. On the two-kernel basis the saving is 0.4us, on the totals basis 4.7us, so the decode sign is basis-dependent. Reconciled arithmetic in README, "Removing the pre-kernel". The prefill losses are unaffected, being three orders of magnitude larger than the 4.6us basis shift, so the shipping decision does not turn on it. Attention consumes an identical buffer either way, so this delta is the whole effect. This **refutes** the earlier projection that the fusion would turn the SWA prefill loss into a win. Ships off by default (`INKLING_TURBO_FUSED_SHEAR=1` to enable) and should stay off for prefill. journal/remote/validate_s26_h100x1/microbench_presheared_splitkv_modal_h100x1.json |
| U2 | rel-attn split-KV decode | null | null | null | null | null |
| U3 | quantized paged KV | null | null | per-op 2/2 on sm_120 + sm_90/H100 + sm_80/A100 (2026-07-20/23); 32/32 pending | null | null |
| U4 | router+dispatch fusion | null | null | null | null | null |
| U5 | QKVR fused GEMM | null | null | null | null | null |
| U6 | graphs+overlap | n/a (timeline) | null | null | null | null |
| U7 | batch-aware dispatch | null | null | null | null | null |

## Corrections

Numbers in this file have been wrong three times. All three are recorded rather
than quietly edited, because a ledger that only ever agreed with itself would not
be worth reading.

| date | what was wrong | how it was found | where it is written up |
|---|---|---|---|
| 2026-07-24 | The headline divided our kernel by `relproj`/`relprojT`, which are **our own** abandoned prototypes, not day-0 paths. It also omitted the measured `score_mod` prefill of 4799.4 us and quoted 3.2x instead of the true 1.45x, and never disclosed that we are 1.28x slower on sliding-window prefill. **Disambiguation, added 2026-07-25, because grepping this repository for `3.2x` returns two different things:** the withdrawn number is a *speedup claim*, ours divided by our own abandoned `relprojT` at prefill. A **different** 3.2x stands and is unrelated, the session-4 `score_mod`-against-plain-attention *headroom* measurement, which is the cost of the day-0 callback and is what motivated the unit. It appears in the title of journal/u2-hopper-design.md and in journal/remote/README.md. The word to check is headroom. | An agent cross-checking citations against the raw artifacts | README, "What we do not claim" |
| 2026-07-25 | The projection that removing the `ShearingBias` pre-kernel would turn the sliding-window prefill loss into a win. It was labelled arithmetic, and it was wrong: the fused writer costs more than the kernel it removes. | Measured it on an H100 (session 26) instead of leaving it projected | README, "Removing the pre-kernel" |
| 2026-07-25 | Every decode speedup ratio in this file and on the front page: 2.7x, 2.8x, 2.5x at session 25 and 2.6x at session 24. They divided our kernel by `score_mod` while our kernel was applying the relative-position bias to one KV block out of 64 or 512 instead of ten. The timings were real; the comparison was not like-for-like. Withdrawn in place, not deleted, and superseded by a post-fix re-measurement of 2.66x / 2.75x / 2.10x from a run whose own parity gate covers the shape family being timed. The withdrawal stands even though the new numbers are close, because proximity by accident is not evidence. | Deriving the shear layout contract in its general form for the in-kernel shear design, which exposed `128 * (m_block + 1)` as a `seqlen_q == seqlen_k` specialisation. No gate went red, because no gate looked | [journal/regression-sm90-bias-shift.md](journal/regression-sm90-bias-shift.md), README "What is measured" and "What is not measured" |

## Reproducibility incidents

| date | what happened | effect on published numbers | write-up |
|---|---|---|---|
| 2026-07-25 | `3b78fc6` moved the `n_block` and `page_idx` bindings inside a non-`const_expr` `if` body, which the CuTe DSL traces as a separate scope. Every sm_90 attention call then failed at trace time with "cannot access local variable 'n_block'". The commit's own message said "unvalidated" and it was the tip of public `main` for several hours. | **None of the numbers were wrong.** They were measured on a file byte-identical to `f585227` and remain reproducible from it. They were **not** reproducible from `3b78fc6`, which cannot compile a single sm_90 attention call. Fixed by hoisting both bindings back to function scope. | [journal/regression-sm90-n-block.md](journal/regression-sm90-n-block.md) |
| 2026-07-25 | `flash_fwd_sm90.py:1232` (at `de7c795`, the tip of public `main`) computed the sheared-bias tile shift as `padded//tile_n - (128*(m_block+1))//tile_n`. `128*(m_block+1)` is the `seqlen_q == seqlen_k` specialisation of `128*n_block_max`; the two are equal only for full prefill with `window_right` 0. Because `apply_rel_bias_sm90` guards on `0 <= tile_idx < bias_num_tiles`, the effect was omission rather than drift: at batch-1 decode with 64K KV the shift came out +9 instead of -502, so 1 KV block of 512 got a bias tile, it was the oldest, and it got the wrong band. Chunked prefill failed identically. Inkling's learned relative-position term was effectively absent from decode on Hopper for the life of the kernel. Three gates all stayed green: `parity_fa4_rel.py`'s three cases are all `cu_seqlens_q == cu_seqlens_k`, the decode microbenchmarks time without inspecting output, and `gate_logit_parity.py` ran `max_tokens=0` with `echo=True` so its 2369-token match is prompt echo, which its own docstring says cannot differ under one tokenizer. | **The prefill numbers are unaffected and reproducible**, because `n_block_max == m_block + 1` holds there, so the fixed kernel computes the same shift. **Every decode ratio is withdrawn**: 2.7x, 2.8x, 2.5x (session 25) and 2.6x (session 24). Per-op parity 3/3 stays true for the family it ran. The full-model 32/32 token match stays true and covers prompt positions only. Fixed in `eb1e487` and validated on an H100 in `e9857de`: `parity_rel_chunked_decode` 7/7 on the fix and, on a deliberately re-broken control, **2/7 as the artifact records it** (`passed: 2`, 5 failed) because `TOL_MEAN` was 5e-3 in that run and the broken `decode_global_ctx4095` case slipped through at 3.28e-03; `TOL_MEAN` is now 5e-4, under which a replay of the same per-case numbers gives 1/7, but that 1/7 is a re-derivation and not a recorded run. The decode ratios re-measured at 2.66x / 2.75x / 2.10x, so the claim survives lower. The withdrawn rows stay withdrawn regardless, because they were unfounded rather than merely imprecise. The same specialisation was live in `flash_fwd.py:917-919` and `:1359`, the sm_80 and sm_120 path, and was fixed there in `9b63979` without a GPU; what that cost the Ampere tile sweep is its own row below. | [journal/regression-sm90-bias-shift.md](journal/regression-sm90-bias-shift.md) |
| 2026-07-25 | `harness/tune_sm80.py`, the one harness this project cites as enforcing parity before a timing is reported in code rather than by habit, timed a different shape family from the one it checked. `CASES:48-49` time decode shapes, `T_q=1` against `T_k=65536`, built at `:65-66` and `:73-74`. `parity_ok()` built ONE `cu_seqlens` at `:102` and passed it as both `cu_seqlens_q` and `cu_seqlens_k` at `:105-106`, so it verified 512 query rows against 512 keys. The generic kernel `flash_fwd.py` carried the `128*(m_block+1)` shear-shift specialisation, which is an identity on 512-on-512 and wrong on 1-on-65536, so the gate was green about the one family the kernel got right and released timings for the family it got wrong. Its guard is lower-bound only (`if n_block >= bias_k_min_tile`), so unlike sm_90 the bias copies all still happened, from addresses mostly outside the bias tile domain: 4 of 2048 KV blocks read in-domain at `tile_n=32` and 2 of 1024 at `tile_n=64`, so the two configurations being compared were not even doing the same wrong work. | **The Ampere tile-sweep percentages are withdrawn**: 10.1% and 18.2% from `journal/remote/tune_sm80_a100.json`, and the journal-only 18.7% post-deploy re-run. The artifact is kept and its absolute timings stand as timings. **Unaffected and still standing**: both prefill rows of the same sweep (8192-on-8192, the family the shift got right), the `tile_n=128` collapse, and the `sm_80` **support** claim, which never depended on a percentage. The support claim now carries two caveats: its parity evidence is `seqlen_q == seqlen_k` only, and as of 2026-07-25 it is also single-sequence only and is not a serving claim. Re-measurement is blocked on a code question: the port of the shear-shift fix into `flash_fwd.py` (`9b63979`) multiplies a `tile_n`-unit block count by the writer's fixed 128, which is correct only at `tile_n == 128`, and the generic path selects 32 or 64. A second form superseded it in `b5f0f7e`, and **no gate in this repository can distinguish either form from the writer's contract**, because every gated `seqlen_k` is a multiple of 128 and the contract only differs when it is not. **Nothing on sm_80 has run since.** The `b5f0f7e` form did run on an sm_120 5090 on 2026-07-25 and its single-sequence gates are green, which is the sm_120 row above; that is a different architecture and does not re-measure the Ampere sweep. | [journal/regression-ampere-tile-sweep.md](journal/regression-ampere-tile-sweep.md) |
| 2026-07-25 | Two single-element tuples in `kernels/tml_fa4_modified/interface.py` (`t.shape == (batch_size)`, `.shape == (1)`) were still damaged by the em-dash cleanup regex, at sites the repair in `8c91a86` missed. A `torch.Size == int` comparison is always False, so both asserts fire for any caller passing `scheduler_metadata`. | No published number depends on that path. Fixed, and a tree-wide sweep for the pattern now returns zero. | same file |
| 2026-07-25 | `harness/parity_rel_varlen_batch.py` was run for the first time, on a local RTX 5090 Laptop (`sm_120`, capability 12.0, torch 2.11.0+cu130), and scored **1 of 12**. Only the single-sequence control passed. The first multi-sequence case died with `torch.AcceleratorError: CUDA error: an illegal memory access was encountered` / `cudaErrorIllegalAddress (700)`, reproduced under `CUDA_LAUNCH_BLOCKING=1` so the fault is at the launch that caused it, and every later case failed as a cascade because a 700 poisons the CUDA context for the rest of the process. Isolated one variable at a time in separate subprocesses: `[512]`, `[256]`, `[137]` and `[256, 137, 64]` all pass, while `[256, 256]` and `[128, 128]` fault. So it is not "more than one sequence"; the two failing shapes are the two whose `total_q` is an exact multiple of 128. That is a correlation over six data points and a hypothesis, not a root cause. **Not the harness**: it passes the documented varlen contract, the library allocates and shears the padded buffer itself, and the interface's own shape assertion passes, so a kernel launched and read out of bounds rather than an assert firing. **Not the shear-shift work**: the failing shapes have zero cached context, so `seqlen_q == seqlen_k` per sequence, the family every form of the shift agrees on. **Not `sm_90`**: `microbench_attn_day0.py`'s `batched_decode_case` builds 32 true sequences and has produced H100 timings repeatedly, most recently 14697.5 us/iter. This is a `flash_fwd.py` defect, the file `sm_80` and `sm_120` share. **Not fixed.** | **No published number is wrong.** Every parity case and every timing in this file was taken with one sequence in the batch, and single-sequence calls are unaffected. What it costs is the **scope** of a claim: the `sm_80` support claim is narrowed to single-sequence calls and is explicitly not a serving claim, since vLLM serving batches sequences into one varlen call on every step. The narrowing is applied in the `sm_80` row above, in README claim 1, in the README architecture table and in docs/METHODOLOGY.md. The fault is **expected** on A100 because the file is shared, and that is labelled as an expectation everywhere it appears, because no A100 has been available to confirm it. The `sm_90` numbers are untouched. Root-causing this **gates any `sm_80` or `sm_120` serving claim**, and it is README item 1 in "What comes next" because the repro is six lines of shapes and runs on a laptop for nothing. | [journal/regression-sm120-varlen-illegal-address.md](journal/regression-sm120-varlen-illegal-address.md) |

## last_error

null
